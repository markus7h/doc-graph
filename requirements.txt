# LightRAG als Library (nicht [api] – wir brauchen den HTTP-Server nicht)
lightrag-hku>=1.4.9
# OpenAI-Binding von LightRAG (spricht den OpenAI-kompatiblen llama-server)
openai>=1.40
# MCP Python SDK mit FastMCP (streamable HTTP)
mcp>=1.9.0
httpx>=0.27
aiofiles
